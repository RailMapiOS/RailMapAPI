# Docker Compose file for Vapor
#
# Install Docker on your system to run and test
# your Vapor app in a production-like environment.
#
# Note: This file is intended for testing and does not
# implement best practices for a production deployment.
#
# Learn more: https://docs.docker.com/compose/reference/
#
#   Build images: docker-compose build
#      Start app: docker-compose up app
#       Stop all: docker-compose down
#
version: '3.7'

x-shared_environment: &shared_environment
  LOG_LEVEL: ${LOG_LEVEL:-debug}
  # `:?` makes Compose fail fast if the var is missing — better than silently
  # starting an unauthenticated server.
  API_AUTH_TOKENS: ${API_AUTH_TOKENS:?API_AUTH_TOKENS must be set in .env}

services:
  app:
    image: rail-map-a-p-i:latest
    build:
      context: .
    env_file:
      - .env
    environment:
      <<: *shared_environment
    restart: unless-stopped
    ports:
      # Bind to loopback only: the API is never reachable from the LAN or the
      # internet directly — cloudflared (running on the host) is the only client.
      # Host port 8090 because 8080 is taken by Zigbee2MQTT on the Pi.
      - '127.0.0.1:8090:8080'
    # user: '0' # uncomment to run as root for testing purposes even though Dockerfile defines 'vapor' user.
    command: ["serve", "--env", "production", "--hostname", "0.0.0.0", "--port", "8080"]
